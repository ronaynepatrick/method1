<%= @passage_id %>
<%= form_tag :save_passage do%>
	<%= cktext_area :passage, :text, :value=>@pass_edit.passage, :ckeditor => {:uiColor => "#000000", :toolbar => "mini" } %><br>
	add line break:  <%= check_box_tag 'line' %> <br>
	<br>
	<%= submit_tag("Save Changes") %>
<% end %>